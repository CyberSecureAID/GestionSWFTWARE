/* ============================================================
   FinFlow — main.js
   Punto de entrada: render inicial de la app.
   ============================================================ */

/* ---------- Init ---------- */
renderSidebarNav();
renderWelcome();
setSaveDot('unsynced', 'Sin guardar');
